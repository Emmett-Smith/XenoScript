# gotcha: an argumentless FOR loops over everything else on its own line
# -- a model that puts the final WRITE on the FOR's own line will print
# it every iteration instead of once at the end.
grade: compile_and_run
max_iterations: 4
tags:
- gotcha
