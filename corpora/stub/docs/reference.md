# Reference

## Error Codes

- `E041` — literal `FAIL` detected in source (stub verifier only; a real
  toolchain's error codes come from its own grammar).

## Glossary

The `end_platform` keyword closes a `platform` block opened with
`platform`. The `inherit` keyword pulls settings from another platform.
